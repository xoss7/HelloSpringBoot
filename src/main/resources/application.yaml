ept:
  git:
    auth:
      token:
        duration: 3600
        length: 24
        char: qwertyuiopasdfghjklzxcvbnm1234567890-._#!^
    contact:
      nom: ${ADMIN_NOM:Fall}
      prenom: ${ADMIN_PRENOM:Adama}
      email: ${ADMIN_EMAIL:adafall@ept.edu.sn}
spring:
  application:
    name: HelloSpringBoot
  datasource:
    username: ${DIC2_DB_USER}
    password: ${DIC2_DB_PWD}
    url: ${DIC2_DB_PWD}
  profiles:
    active: ${DIC2_PROFILE:th}

  config:
    location: ${CONFIG_DIR}